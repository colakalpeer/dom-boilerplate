let cont = document.getElementById("container");
let msg = cont.getElementsByClassName("message");

console.log(msg);

/* we can call the id we opened with getElementById and then we can access the classes
in it with getElementsByClassName using the value (cont) we have given. */